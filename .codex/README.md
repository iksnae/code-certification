# Codex Workspace Assets

This workspace exposes Codex command and skill assets via:

- `./commands` -> `../.claude/commands`
- `./skills` -> `../.claude/skills`

The symlinked layout keeps Claude and Codex behavior aligned while avoiding duplicate files.
